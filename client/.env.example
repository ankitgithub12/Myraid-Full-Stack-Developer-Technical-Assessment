# Local development — talks to local backend
VITE_API_URL=http://localhost:5000/api

# Production — set this in Render Static Site dashboard (not in this file)
# VITE_API_URL=https://myraid-server.onrender.com/api
